# === 앱 ===
APP_NAME=go-fiber-starter
# local | dev | prod (prod + sqlite 조합은 시작 시 거부됨)
APP_ENV=local
APP_PORT=8080
# debug | info | warn | error (debug에서 GORM SQL 로그 활성화)
LOG_LEVEL=info

# === 데이터베이스 ===
# postgres | mysql | sqlite
DB_DRIVER=sqlite
# sqlite: 파일 경로 / postgres: postgres://user:pass@localhost:5432/db?sslmode=disable
# mysql:  user:pass@tcp(localhost:3306)/db?parseTime=true
DB_DSN=data/app.db
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5
DB_CONN_MAX_LIFETIME=30m
# dev 편의용 AutoMigrate. prod(postgres/mysql)에서는 true 금지 — ./cmd/migrate 사용
DB_AUTO_MIGRATE=true
DB_SLOW_QUERY_THRESHOLD=500ms

# === HTTP/미들웨어 ===
CORS_ALLOWED_ORIGINS=*
RATE_LIMIT_PER_MINUTE=120
HTTP_READ_TIMEOUT=15s
HTTP_WRITE_TIMEOUT=15s
HTTP_IDLE_TIMEOUT=60s

# === 종료 대기 ===
SHUTDOWN_GRACE_PERIOD=10s
